# Open Questions

Last updated: 2026-09-02

This file is for user-owned or architecture-sensitive questions. Do not silently decide these during implementation.

## Immediate

| Area                              | Question                                                                                                               | Why It Matters                                                                                                                                                                                                                                                                           | Status                                                                                                                                         |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| Clerk SMS delivery                | Which SMS provider should own `sms.created` delivery if Clerk SMS delivery is disabled?                                | `api_v2` accepts and ledgers `sms.created`, but the current delivery stack is Resend/email-only. Disabling Clerk SMS before an SMS provider exists would drop OTP SMS messages.                                                                                                          | Open; keep Clerk SMS delivery enabled until provider is chosen and implemented.                                                                |
| Moderation media durations        | Where should trusted audio/video duration metadata be captured for plan-limit enforcement?                             | `MediaAsset` has byte size and content type, but no duration field. The first implementation enforces per-submission attachment count, image monthly count, and image byte caps; audio/video duration caps need duration metadata or provider-side accounting before strict enforcement. | Open; do not invent duration metadata without a schema/product decision.                                                                       |
| Production/public-host activation | When should the first protected production release, contract artifact, and exact/wildcard public-host DNS cutover run? | The expand-state implementation and runbook are ready for staged verification, but public hosts remain unactivated; first execution changes production, provider, DNS, and (in a separate artifact) database-contract state.                                                             | Await sober, explicit user approval for each gate; do not dispatch, apply a contract migration, or alter AWS/Cloudflare/Vercel/DNS implicitly. The 2026-08-31 launch plan targets the Aug 29–31 window; operator provisioning must start by Aug 20 (`docs/plans/2026-08-08-release-plan.md`). |
| EMAIL_ENABLED at launch           | Does the user approve flipping `EMAIL_ENABLED=true` for the production launch (Resend domain verified, `EMAIL_FROM`/`EMAIL_REPLY_TO` chosen)?  | The deploy contract mandates `false` "until separately approved"; with it off, sign-up verification, invites, notifications, thank-yous, and the new request feature are all silently suppressed — the product loop does not function.                                                       | RESOLVED 2026-08-09: approved. Flips true at the Aug 22–24 staging rehearsal and stays on through launch. Sender identity (`EMAIL_FROM`/`EMAIL_REPLY_TO`) still chosen in the operator batch. |
| Marketing site / apex ownership   | What serves `semblia.com` (apex) on 2026-08-31, and who builds the landing/waitlist page?                              | No marketing site exists in this repo; the apex also carries the legacy `/wall/:slug` adapter expectation. A public launch with no landing page is a go-to-market gap, not a code gap.                                                                                                     | RESOLVED 2026-08-09: in-repo — `apps/marketing` serves the apex (WS-I; landing + pricing + legal basics; no waitlist — the landing CTA is sign-up at `app.semblia.com`), and the `/wall/:slug` adapter is removed under the subdomain-only hosting doctrine. Copy/voice still needs user input.  |
| Admin app at launch               | Deploy `apps/admin` to production, or relax the hard `ADMIN_CLERK_*` production requirement until it deploys?          | The env validator refuses production boot without admin Clerk keys, but admin has no deployment path — and plan records (source of pricing truth) are created there.                                                                                                                       | Decide with the operator provisioning batch (already past its Aug 20 start). WS-E kept the validator's requirement and the first-deploy runbook documents that the admin Clerk *application* must exist either way; the open half is only whether the admin *app* deploys to admin.semblia.com. |
| Staging rehearsal environment shape (WS-E5) | Which scratch hostnames does the rehearsal use, and do the scratch AWS/Vercel resources live in separate cloud accounts or as separate resources inside the production accounts? | The first-deploy runbook forbids rehearsing against production DNS or the `DEPLOY_PRODUCTION` workflow (both are production-only by design), so the rehearsal needs concrete scratch equivalents before it can start. Provider isolation itself is no longer a choice: the runbook mandates non-production Clerk/Razorpay/Resend/AWS credentials and a live-key gate check (2026-09-02, after CodeRabbit flagged the ambiguity on PR #67). | Open — operator decision, needed before the rehearsal starts. The Aug 22–24 window has passed; the release calendar is being re-dated. |
| Customer Clerk production scope (WS-E2) | Should production validation require more than `CLERK_SECRET_KEY` — i.e. `CLERK_AUTHORIZED_PARTIES` (the guard passes `undefined` azp today), `CLERK_WEBHOOK_SIGNING_SECRET` (webhook route dead without it), `CLERK_PUBLISHABLE_KEY`? | WS-E2 implemented the plan's literal mandate (`CLERK_SECRET_KEY` required, mirroring nothing else). A production boot with the secret but without authorized parties weakens azp checking silently; without the webhook secret, Clerk webhooks fail at request time. | Open — security-posture decision for the user; extending the required set is a two-line change in `apps/api/src/config/env.ts` once decided. |

## Watch Items

| Area                              | Watch Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Handling                                                                                                                                                                                                                                                                                                                         |
| --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Inbound provider activation       | X plan access, LinkedIn product approval, Google consent/API verification and resource permissions, and Vimeo token ownership are external prerequisites for implemented inbound adapters. Trustpilot is manual/spreadsheet-only because its deletion reconciliation and provider obligations are not implemented.                                                                                                                                                                          | Keep unavailable adapters setup-required or manual/spreadsheet-only in the catalog. Keep Trustpilot manual/spreadsheet-only until deletion reconciliation and provider obligations are implemented; do not add Trustpilot API sync before then.                                                                                  |
| `web_v2` wiring                   | Current UI mocks may diverge from backend-canonical contracts.                                                                                                                                                                                                                                                                                                                                                                                                                              | Track deltas explicitly; update UI to API instead of bending API to mocks.                                                                                                                                                                                                                                                       |
| Public trust                      | HMAC and Origin trust must stay separate.                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Failed HMAC must not fall through to Origin.                                                                                                                                                                                                                                                                                     |
| PII                               | Display-safe rows must not regain sensitive public-submit metadata.                                                                                                                                                                                                                                                                                                                                                                                                                         | Keep private metadata writes and serializers under review.                                                                                                                                                                                                                                                                       |
| Docs                              | Older docs can still contain historical scope/start-point language.                                                                                                                                                                                                                                                                                                                                                                                                                         | Treat `docs/continuity/` as canonical; edit old docs only when they mislead current handoff behavior.                                                                                                                                                                                                                            |
| Feedback integrity                | Agent/API/integration write surfaces must not mutate original collected feedback.                                                                                                                                                                                                                                                                                                                                                                                                           | Keep source submissions immutable; allow annotations, moderation, publish state, and exports. Rebuild a presentation/projection workflow only if a future projection is intentionally added.                                                                                                                                     |
| Native integrations               | Slack/Notion/Linear/GitHub behavior must stay one-way and provider-adapter scoped for launch.                                                                                                                                                                                                                                                                                                                                                                                               | Do not import remote edits, sync provider membership, or make provider webhook state part of core Semblia state unless the user explicitly reopens integration scope.                                                                                                                                                            |
| Rebuilt forms browser uploads     | UPDATED 2026-08-08: the audit verified `browser.ts` DOES consume `data-presign-url` — it presigns, PUTs with required headers, and rewrites answers to media-asset ids; the earlier "does not consume" note was stale. The remaining upload gaps are narrower: the PRODUCT_FEEDBACK template offers `application/pdf` that the server allowlist rejects, the video size caps disagree (field 200MB vs server 100MB), and no bucket CORS policy is checked in so the cross-origin PUT is unproven against real object storage.                                                            | WS-B3 of `docs/plans/2026-08-08-release-plan.md`: align the type/size contracts, check in the bucket CORS requirement, and add an upload leg to the public-hosting verification before launch.                                                                                                                                   |
| Widget studio parametric pass     | RESOLVED 2026-06-14: the widget-studio freeform-layer question is settled by `docs/plans/2026-06-14-widgets-parametric.md` and the implementation pass. Widgets now use the shared `@workspace/brand-theme` engine, `@workspace/widgets-core` versioned definitions/publish snapshots, production SSR preview fragments, and a tiny Shadow DOM loader package (`@workspace/widgets-embed`).                                                                                                 | Do not reintroduce freeform token/composition controls in the widget studio. The former server-save/direct-navigation tail is also resolved: the current shell hydrates, saves, and publishes through the API-backed widget draft hooks. Hosted wall deployment, custom domains, and AI brand import remain separate follow-ups. |
| Production public surfaces        | `app.semblia.com` currently points at a missing Vercel deployment; the apex, API, forms, and admin hosts have no usable A/AAAA/CNAME answer.                                                                                                                                                                                                                                                                                                                                                | Use `deploy/production/README.md` and the protected release workflow after approval. Complete the Phase-8 form embed loaders and verify public form/widget hosts before declaring launch-ready.                                                                                                                                  |
| Forms custom domains              | Hosted forms can resolve active custom `PublicSurfaceHost` rows through the API, but production custom-domain automation is still stubbed: CloudFront alternate domain names, ACM certificate issuance/validation, DNS ownership checks, and per-tenant rollout/teardown are not implemented.                                                                                                                                                                                               | `apps/forms_runtime` now fails loudly if `formsRuntimeCustomDomains` is passed to CDK. Ship only the Semblia-managed exact/wildcard forms domains until a production custom-domain rollout is designed.                                                                                                                          |
| Dependency hygiene                | The Next/pnpm alignment pass handled the visible `web_v2` Next item. The remaining watch item is the Prisma/advisory review tracked from the billing audit and GitHub issue sync.                                                                                                                                                                                                                                                                                                           | Keep dependency remediation in a dedicated dependency PR instead of blending it into feature/database cleanup work.                                                                                                                                                                                                              |
| Scheduled-switch race             | If a `subscription.activated` webhook for a previously scheduled Razorpay subscription arrives while the user is re-issuing a switch, the new scheduled id may be written before activation promotion completes. Both branches converge on a valid local row.                                                                                                                                                                                                                               | Last-write-wins is acceptable for launch; future hardening could add row-version compare-and-swap on the scheduled slot.                                                                                                                                                                                                         |
| Integrations OAuth provider setup | The 2026-06-07 integrations repair filled the backend/control gaps locally: revoke/delete, re-enable, Clerk-OAuth token/scope verification, and provider resource discovery are implemented for Slack, Notion, Linear, and GitHub. The remaining production dependency is platform-side OAuth app/provider configuration in Clerk (provider credentials, redirect URLs, and requested scopes).                                                                                              | Do not push this setup onto customers or reintroduce manual destination-id entry as the normal flow. Customers should only authorize the provider, pick a server-discovered destination, disable/enable/revoke from Semblia, and control what remains connected.                                                                 |
| Email DNS propagation             | Zoho admin can lag after Cloudflare DNS is updated. During setup, public DNS showed the Semblia MX records while Zoho still reported "Yet to point MX Records."                                                                                                                                                                                                                                                                                                                             | Recheck Zoho after DNS propagation before treating inbound mail setup as failed. Keep Zoho as the inbound workspace mailbox and Resend as transactional outbound unless the user reopens the provider split.                                                                                                                     |
| Account-defaults DB cleanup       | The 2026-06-13 removal stripped the user-facing account-defaults surface end-to-end at the code/contract level (web page + hooks + client, `GET/PATCH /v2/account/defaults`, the user read/write service methods, and project-creation now uses `parseAccountDefaults(null)`). The physical schema is intentionally untouched: `User.defaults`, `User.accountDefaultsLogoAssetId` / `accountDefaultsLogoAsset`, and `MediaAssetPurpose.ACCOUNT_DEFAULTS_LOGO` remain, now unread/unwritten. | Drop the column/relation/enum value (+ migration) and prune the now-internal-only `V2AccountDefaultsDTO` / `V2UpdateAccountDefaultsBody` family in a dedicated DB-hygiene pass (Codex-owned), separate from feature work, to avoid migration-drift risk mid-session.                                                             |
| Customer entity pages             | Senja's reference UI (2026-08-02 collection-IA pass) models a per-customer page — profile rail (email, company, socials) plus a timeline of everything that person submitted, with actions like "invite to a form". Semblia's record pane serves moderation only; there is no person-centric surface aggregating a customer's responses across forms.                                                                                                                                       | Product-scope decision for the user: adopting a customer/CRM-lite entity changes the data model's centre of gravity (person vs response). Do not build it as a side effect of UI work.                                                                                                                                          |
| Team-seat reservation race (WS-C1) | The `teamMembers` plan-limit check on invite-create and accept is check-then-act: two concurrent invite-creates on the same project can both observe a free seat and both commit, over-reserving by a few. The dangerous half — a permanent accept deadlock — was fixed this session (accept counts ACTIVE members only, so surplus pending invites simply do not all get accepted; it self-heals). Flagged by CodeRabbit + CodeScene on PR #65 and dispositioned there. | Bounded, self-healing over-reservation is acceptable for launch (matches the last-write-wins precedent above). Post-launch hardening: serialize the seat check + write per project (pg advisory lock or serializable tx + retry). Not a launch blocker. |
| Non-prod unsubscribe headers (WS-C) | `EMAIL_UNSUBSCRIBE_SECRET` / `API_PUBLIC_URL` are required only in production, so a non-prod deploy with `EMAIL_ENABLED=true` sends project-voiced mail with a null unsubscribe URL / no `List-Unsubscribe` header (the send still proceeds — blocking auth/thank-yous on a missing secret would be worse). | Deliberate scope boundary: local dev does not reach real external recipients meaningfully. The Aug 22–24 staging rehearsal runs `EMAIL_ENABLED=true` against real-ish recipients, so the operator batch must set the unsubscribe secret + `API_PUBLIC_URL` on staging (already in the WS-E runbook). |
| Per-tenant email budget (WS-D) | The delivery pipeline's only quota is the global `EMAIL_DAILY_LIMIT` (platform-wide, blind to who spends it). WS-D bounds its own surface (50 recipients/compose, 200 request-recipients/project/UTC-day, 409 beyond), but thank-yous, published-notices, and future outbound features still draw from the shared global budget with no per-tenant accounting. | Post-launch hardening: a per-project daily counter (or plan-tier `limits` key) consulted in `EmailDeliveryService.isDailyLimitReached`, replacing WS-D's flat service-level bound. Not a launch blocker — the flat bound caps the only user-triggerable fan-out. |
