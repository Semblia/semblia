# Locked Decisions

Last updated: 2026-07-17

This file records decisions that future sessions should treat as settled unless the user explicitly reopens them.

## Operating Decisions

| Date       | Decision                                                                                                                                                                                                                                                                  | Owner | Source                                      |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | ------------------------------------------- |
| 2026-04-30 | Codex/Claude acts as senior engineer/orchestrator: owns security, quality, architecture, contracts, and verification; delegates simple to medium-complexity work to OpenCode agents when available.                                                                       | User  | `AGENTS.md`, Claude memory migration        |
| 2026-04-30 | User retains business and architecture ownership. Stop and consult before making product or architecture decisions.                                                                                                                                                       | User  | `AGENTS.md`                                 |
| 2026-04-30 | Stay on `revamp/v2`; use one checkpoint commit per named phase or subphase. Subagents do not commit.                                                                                                                                                                      | User  | `apps/api_v2/docs/orchestration/handoff.md` |
| 2026-05-02 | `docs/continuity/` is the canonical durable memory location for progress, decisions, open questions, and tool state.                                                                                                                                                      | Codex | This continuity setup                       |
| 2026-05-02 | `docs/continuity/` is the only durable project-memory workflow. Do not add external documentation mirrors or secondary handoff stores unless the user explicitly reopens this.                                                                                            | User  | Current continuity cleanup directive        |
| 2026-05-13 | Use `github-copilot/gpt-5.4-mini` with the highest available variant for delegated codebase exploration when OpenCode delegation is useful and available.                                                                                                                 | User  | Current web wiring continuation             |
| 2026-05-31 | Use native Codex subagents through the `multi_agent_v1` flow for delegated exploration, scaffolding, bounded implementation, and non-critical delegated review. The orchestrator remains the senior engineer, owns verification and commits, and subagents do not commit. | User  | Current orchestration update                |
| 2026-07-03 | Every new codebase task should run on a fresh branch and finish through a pull request rather than direct `main` edits. If PR bots or reviewers need follow-up and the task is long or usage-limited, schedule a later unblock pass with at least a 5-hour delay instead of keeping the user in the loop synchronously. | User | Current security hardening session |

## Product And Architecture Decisions

| Date       | Decision                                                                                                                          | Notes                                                                                                                                                                                                                                                                                                                                                                                                             | Source                                     |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ |
| 2026-05-02 | Backend and database move first; `web_v2` adapts to the new contracts.                                                            | Do not constrain API/database shape to current mocks.                                                                                                                                                                                                                                                                                                                                                             | Consolidated gap map                       |
| 2026-05-02 | Visible auxiliary surfaces remain in v2 scope.                                                                                    | Analytics, notifications, billing, and API keys are real product areas.                                                                                                                                                                                                                                                                                                                                           | Consolidated gap map                       |
| 2026-05-02 | Project access is API-driven through an `access` block on project responses.                                                      | Use backend hardcoded role-to-capability rules, not client-side role reimplementation.                                                                                                                                                                                                                                                                                                                            | Consolidated gap map                       |
| 2026-05-02 | Forms and widgets use server-side drafts as the durable source of record.                                                         | Browser local state is only a short-lived UX buffer.                                                                                                                                                                                                                                                                                                                                                              | Consolidated gap map                       |
| 2026-05-02 | Draft topology is one shared draft per resource with optimistic concurrency.                                                      | Include version, updatedBy, updatedAt; stale writes fail clearly.                                                                                                                                                                                                                                                                                                                                                 | Consolidated gap map                       |
| 2026-05-02 | Public submit trust stays route-aware and mode-specific.                                                                          | Browser Origin trust and HMAC signing-secret trust remain separate. Direct testimonial-style public submit is a product route, but it now persists `CollectionFormSubmission` through `PublicSubmitSurface.DIRECT_SUBMISSION`; hosted/form submit persists through `PublicSubmitSurface.FORM`.                                                                                                                    | Consolidated gap map; 2026-06-03 cleanup   |
| 2026-05-02 | Failed HMAC must hard-reject and must not fall through to Origin validation.                                                      | HMAC is evaluated first when `X-Semblia-Signature` is present.                                                                                                                                                                                                                                                                                                                                                     | Consolidated gap map                       |
| 2026-05-02 | API keys are not public submit trust.                                                                                             | API keys are for authenticated private/project APIs only.                                                                                                                                                                                                                                                                                                                                                         | Consolidated gap map                       |
| 2026-05-02 | Trust and host configuration are normalized.                                                                                      | Use first-class trusted origins, signing secrets, and hosted/public surface models.                                                                                                                                                                                                                                                                                                                               | Consolidated gap map                       |
| 2026-05-02 | Default hosted domains are derived from project slug and feature.                                                                 | Use wildcard DNS plus app-level routing, not per-project DNS mutations.                                                                                                                                                                                                                                                                                                                                           | Consolidated gap map                       |
| 2026-05-02 | API resolves and authorizes public hosts; `web_v2` renders public page experiences.                                               | Keep trust and multi-tenant host logic centralized in API.                                                                                                                                                                                                                                                                                                                                                        | Consolidated gap map                       |
| 2026-05-02 | Custom domains are modeled now, but self-serve verification is deferred.                                                          | Schema should support custom domains before UX/cert automation exists.                                                                                                                                                                                                                                                                                                                                            | Consolidated gap map                       |
| 2026-05-02 | Every successful form submission creates a `CollectionFormSubmission`.                                                            | Submissions are canonical for answers, ratings, trust metadata, and analytics inputs.                                                                                                                                                                                                                                                                                                                             | Consolidated gap map                       |
| 2026-05-02 | Billing starts as read-only projections.                                                                                          | Plan, subscription, and invoice history are visible; payment mutations remain disabled until provider behavior is settled.                                                                                                                                                                                                                                                                                        | Consolidated gap map                       |
| 2026-05-02 | Analytics must include event capture, not only read endpoints.                                                                    | Public form views, widget loads, submission impressions, and related events should populate real analytics.                                                                                                                                                                                                                                                                                                       | Consolidated gap map                       |
| 2026-05-03 | Keep Clerk as the primary auth provider.                                                                                          | Clerk owns identity, sessions, login OAuth, organization membership, and connected-account OAuth where suitable; Semblia owns authorization, project ownership, API credentials, integrations, and delivery behavior.                                                                                                                                                                                              | Current auth architecture discussion       |
| 2026-05-03 | Use Clerk Organizations as the workspace/account layer.                                                                           | Semblia stores a local organization mirror keyed by Clerk organization ID; one Clerk organization can own many Semblia projects.                                                                                                                                                                                                                                                                                    | Current auth architecture discussion       |
| 2026-05-03 | Use an Appwrite-like control-plane hierarchy without copying Appwrite's generic product surface.                                  | Organization owns projects; projects own credentials, public surfaces, integrations, submissions, testimonials, and analytics. Semblia remains domain-specific to feedback/testimonials.                                                                                                                                                                                                                           | Current product architecture discussion    |
| 2026-05-03 | Keep launch project permissions simple.                                                                                           | Clerk org admins manage credentials, integrations, dangerous settings, members, and billing; org members can operate project content. Later project-level member roles were added in Settings.                                                                                                                                                                                                                    | Current auth architecture discussion       |
| 2026-05-03 | Build the launch product as integration-first, not conservative-minimal.                                                          | Inbound collection, outbound webhooks, CSV export, and thin Slack/Notion/Linear/GitHub exports are differentiators. Avoid deep bidirectional sync at launch.                                                                                                                                                                                                                                                      | Current product architecture discussion    |
| 2026-05-03 | Agent access is a first-class launch pillar.                                                                                      | Ship scoped agent keys and an official MCP adapter over private APIs so AI agents can inspect and manage safe Semblia workflows.                                                                                                                                                                                                                                                                                   | Current product architecture discussion    |
| 2026-05-03 | Original feedback is immutable.                                                                                                   | Agents, API keys, and integrations may annotate, moderate, publish/unpublish when scoped, and export. They must not silently rewrite collected submission content or recreate a presentation/projection workflow unless a future projection is intentionally designed.                                                                                                                                            | Current trust architecture discussion      |
| 2026-05-03 | The old control-plane implementation plan is `docs/plans/2026-05-03-auth-integrations-agent-access-implementation-plan.md`.       | Historical implementation evidence only; do not use it as a current start point now that legacy codepaths are removed.                                                                                                                                                                                                                                                                                            | Current planning checkpoint                |
| 2026-05-03 | Private API keys and agent keys are project-bound scoped credentials, not public submit trust.                                    | Store only scrypt hashes; show raw secrets only once on create/rotate; map valid credentials into `ActorContext`; keep read integration/export/webhook scopes below write-level `MANAGE_INTEGRATIONS`.                                                                                                                                                                                                            | API/agent key implementation checkpoint    |
| 2026-05-06 | Public submit idempotency is surface-scoped and only completed responses can replay.                                              | `PublicSubmitIdempotency` uses project + surface + key uniqueness; form and direct-submission keys cannot collide; in-flight duplicate keys return `409 Conflict` instead of replaying the placeholder response.                                                                                                                                                                                                  | Security audit refresh; 2026-06-03 cleanup |
| 2026-05-06 | Public submit throttling is mode-specific and counts invalid attempts before returning trust errors.                              | Public list reads, browser submits, and HMAC submits use separate named buckets. Malformed Origin/HMAC attempts use a slug + IP invalid-submit bucket; if the bucket is blocked, the throttling error wins.                                                                                                                                                                                                       | Security audit refresh                     |
| 2026-05-06 | API-key prefixes are routing hints, not unique authentication proof.                                                              | Authentication checks every active candidate with the same public prefix and accepts only the row whose scrypt hash matches the supplied secret.                                                                                                                                                                                                                                                                  | Security audit refresh                     |
| 2026-05-08 | Feedback integrity APIs separate source feedback from workflow layers.                                                            | `CollectionFormSubmission` answers, rating values, trust metadata, and private metadata remain immutable source records. Agents/API keys can annotate, moderate, and publish/unpublish when scoped. The old display-suggestion workflow was removed with the `Testimonial` projection on 2026-06-03 and must be rebuilt intentionally if needed.                                                                  | Feedback integrity checkpoint              |
| 2026-05-08 | Outbound webhooks and CSV exports are project-scoped async integration primitives.                                                | Webhook endpoints use encrypted one-time secrets and signed async deliveries. CSV exports produce database-backed display-safe artifacts. Provider-specific native integrations landed in a later checkpoint.                                                                                                                                                                                                     | Webhook/export checkpoint                  |
| 2026-05-10 | Native thin integrations are one-way project-scoped export destinations.                                                          | `IntegrationConnection` stores provider config and connected-account metadata; native exports use `ExportDelivery` and provider adapters. Semblia does not import remote edits, sync provider membership, or depend on provider webhooks for core state.                                                                                                                                                           | Native integration checkpoint              |
| 2026-05-10 | The official MCP server is a local stdio adapter over scoped private APIs.                                                        | `@workspace/semblia-mcp-server` uses `SEMBLIA_API_BASE_URL` and `SEMBLIA_AGENT_KEY`, exposes safe tools/resources/prompts, never connects directly to the database, and must preserve project-scoped credential boundaries.                                                                                                                                                                                          | MCP checkpoint                             |
| 2026-05-24 | Billing source of truth is Razorpay Subscriptions, mirrored to local `Subscription`/`Invoice`/`SubscriptionPayment` via webhooks. | Local DB is read-only outside of webhook handlers. Frontend never POSTs to Razorpay directly; the API issues a checkout session and the browser opens Razorpay Standard Checkout. INR-only at launch.                                                                                                                                                                                                             | Billing source-of-truth decision           |
| 2026-05-24 | Plan switching = cancel current at period end + schedule new subscription for next cycle.                                         | No mid-cycle proration, no immediate charge, no downgrade refunds. Lets us avoid Razorpay add-ons and proration math at launch.                                                                                                                                                                                                                                                                                   | Billing source-of-truth decision           |
| 2026-05-24 | No free trial on paid plans. FREE tier remains the always-free baseline; PRO and BUSINESS charge on day 1.                        | Simpler webhook state machine with no trial-end notification surface required for launch.                                                                                                                                                                                                                                                                                                                         | Billing source-of-truth decision           |
| 2026-05-24 | Saved payment methods are read-only, populated from `subscription.charged` events.                                                | Razorpay holds the card mandate; we display brand/last4 from token metadata in webhook payloads. No add-card UI, no add/remove/set-default endpoints. Razorpay Checkout captures card-on-file per subscription.                                                                                                                                                                                                   | Billing source-of-truth decision           |
| 2026-05-28 | BullMQ plus Redis is the launch background job substrate.                                                                         | HTTP API modules enqueue work and the worker entrypoint owns processors and cron maintenance. RabbitMQ remains a future option only if cross-service routing or broker-native semantics become necessary.                                                                                                                                                                                                         | Worker pool implementation checkpoint      |
| 2026-05-28 | Resend is the launch transactional email provider.                                                                                | Email sending is durable through `EmailDelivery`, deterministic BullMQ job ids, and Resend idempotency keys. `EMAIL_ENABLED=false` remains the default until the production domain, sender, and daily quota are explicitly configured.                                                                                                                                                                            | Worker pool implementation checkpoint      |
| 2026-05-28 | Worker cron handlers are worker-only, not shared API-module providers.                                                            | The shared `QueueingModule` exposes locks and telemetry, while `QueueMaintenanceModule` is imported only by `WorkerModule` to avoid duplicate cron execution in HTTP API processes.                                                                                                                                                                                                                               | Worker pool implementation checkpoint      |
| 2026-05-30 | Hosted forms use a separate public runtime app plus a shared renderer package.                                                    | `packages/forms-core` owns shared schema/normalization/view-model/rendering for hosted runtime and `web_v2` studio previews. `apps/forms_runtime` owns public HTTP routing, request signing, local mock dev, and AWS CloudFront + Lambda Function URL deployment; `api_v2` remains the canonical source of truth for resolution, submissions, analytics, notifications, and trust validation.                     | Hosted forms scaffold                      |
| 2026-06-02 | Submission moderation is AWS-first while AWS credits are available.                                                               | For the next 3-4 months, use AWS for most provider-backed moderation: Comprehend toxicity detection for text, Rekognition for images and video moderation, and Transcribe for audio/transcript/toxicity work. Keep local heuristics before provider calls, make all provider work queue-based and budget-gated, and reserve OpenAI/Azure/Google as non-primary fallbacks unless the user reopens provider choice. | Current moderation planning                |
| 2026-06-02 | Provider moderation can add risk annotations, but human moderation remains authoritative.                                         | Moderation runs are queued and persisted; successful provider decisions may approve clean verified submissions or flag risky content, but worker reconciliation must not override reviewer-set `REJECTED` or reviewer-set `APPROVED` states. Raw provider output stays private and review DTOs expose only safe run summaries.                                                                                    | AWS-first moderation implementation        |
| 2026-06-03 | `CollectionFormSubmission` is the only persisted feedback source; no `Testimonial` projection table remains for prelaunch v2.     | The testimonials product routes remain as product surfaces, but they read/write submission-backed DTOs using `submissionId`. Direct public submits create/reuse a project-scoped direct-submissions backing form, submission-owned private metadata, submission media links, and submission analytics. Display-suggestion/projection workflow must be rebuilt intentionally later if needed.                      | Testimonial projection removal             |
| 2026-06-10 | Parametric theming over a freeform form builder (forms v4).                                                                       | The freeform/interactive builder is dropped after 3-4 failed iterations. Forms separate structure (Zod-validated questions/logic), layout (3-5 hand-designed presets, never rearrangeable), and appearance (constrained theme knobs resolved by the AA-clamped derivation engine). Escape hatches are tiered: presets → theme object → headless API. No drag-and-drop, ever.                                      | Notion decision doc; plan `docs/plans/2026-06-11-forms-v4-parametric-theming.md` |
| 2026-06-11 | Form configs are versioned envelopes; publish stamps a derived snapshot.                                                          | `CollectionForm.config` stores a `schemaVersion: 2` `FormDefinitionDoc`; `publishFormDefinition` validates at write time and stamps the resolved theme snapshot so serving never derives or validates at request time. `migrateFormDoc` owns the lossy v1→v2 projection; unknown future versions throw instead of silently projecting. Resolve exposes a sha256 `configEtag` for edge caching.                    | Forms v4 implementation                    |
| 2026-06-11 | Embeds ship as a Shadow DOM custom element with zero framework on the wire and a CI-enforced size budget.                         | `<semblia-form>` (packages/forms-embed) fetches one SSR'd fragment from `forms_runtime` `*/__embed` and mounts it in a shadow root; host CSS cannot bleed in. The loader build fails over 3 KB gzipped (806 B at introduction). The mount strategy is a seam so an iframe mode can be added for hostile hosts. Webfonts are theme opt-in, system fonts default.                                                    | Forms v4 delivery architecture             |
| 2026-06-11 | The freeform layer was hard-deleted, not frozen; rendering is loudly stubbed until preset renderers land.                         | The Collect studio's design controls, the rich-token renderer, and the flow×container×hero model are gone. Hosted pages and embeds serve a marked "being rebuilt" stub; preset render entries throw `FormsV4NotImplementedError`. The widget studio keeps its own freeform controls — that surface is outside this decision and needs its own pass.                                                               | User scope decision this session           |
| 2026-06-11 | Theme telemetry persists as `ProjectActionAudit` rows, not a new analytics table.                                                 | `forms_theme.*` knob events are project-actor activity; the audit table already has actor/action/target/metadata and a `(projectId, action, createdAt)` index. The event contract lives in `forms-core/telemetry`; capture is `POST /v2/projects/:slug/forms/:formId/theme-telemetry` (MANAGE_PROJECT, throttled, batch ≤ 50).                                                                                    | Forms v4 validation plan                   |
| 2026-06-13 | The launch brand/domain is Semblia on `semblia.com`; the previous prelaunch name is retired.                                      | Active code, docs, package names, env vars, public headers, embeds, hosted domains, and visible product copy should use Semblia (`x-semblia-*`, `<semblia-form>`, `@workspace/semblia-mcp-server`, `SEMBLIA_*`, `*.semblia.com`). The historical local checkout folder may still be named by the filesystem, but repo-owned strings should not introduce the old public brand.                                             | Current rebrand session                    |
| 2026-06-13 | Launch email split: Zoho Mail owns human/workspace inboxes, Resend owns transactional app outbound.                               | Cloudflare DNS is configured for Zoho MX/SPF/DKIM/DMARC on `semblia.com` and Resend DKIM plus MAIL FROM on `send.semblia.com`. Cloudflare Email Routing stays disabled unless the routing model is explicitly reopened.                                                                                                                                                                                             | Domain/email setup session                 |
| 2026-06-13 | `web_v2` is full-bleed app-wide; the centered/`contained` rail is retired.                                                        | The `contained` prop (centered `max-w-6xl` rail) was removed from `PageHeader`/`PageBody`/`PageToolbar` and every call site, including the `/projects` workspace-home. Top-level pages fill the content width so band borders line up with content at all viewports. Do NOT reintroduce a centered max-width rail on top-level control-plane pages without the user reopening this. | UI rework session; `docs/ui-rework/2026-06-13-fullbleed-settings-nav/` |
| 2026-06-13 | Project defaults are platform-governed, not user-settable.                                                                        | The account-level defaults feature (form/brand/moderation/visibility applied to new projects) is removed. There is no user-facing read/write surface (web page, hooks, client, and `GET/PATCH /v2/account/defaults` are gone). Project creation derives defaults from the canonical platform constants via `parseAccountDefaults(null)`. The physical `User.defaults` column / `accountDefaultsLogoAsset` / `ACCOUNT_DEFAULTS_LOGO` enum + the now-internal DTOs are a deferred schema-cleanup pass (see `open-questions.md`). | UI rework session; resolves the account-defaults open question |
| 2026-06-14 | Widgets use the same parametric brand-theme architecture as forms.                                                                | `@workspace/brand-theme` is the shared derivation engine; `@workspace/widgets-core` owns widget schema migration, publish snapshots, telemetry contracts, CSS vars, and SSR fragments. `Widget.config` is the canonical versioned envelope and `Widget.publishedSnapshot` is the serve-time snapshot; flat widget columns remain only as compatibility/query mirrors until a dedicated schema-cleanup pass. Public widget fragments are served from `/v2/widget-embeds/projects/:slug/:widgetId/fragment` under the existing project trusted-origin model. Hosted wall pages, custom domains, and AI brand import remain deferred unless explicitly reopened. | Plan `docs/plans/2026-06-14-widgets-parametric.md` |
| 2026-06-18 | Forms ("collection surface") is rebuilt from scratch end-to-end per `docs/raw/forms_redesign_specs.md`; the forms-v4 parametric implementation is superseded.                                                                | Plan `docs/plans/2026-06-18-forms-rebuild.md`. Broader scope: 5 intents, 13-type field system, consent model, conditional flow, immutable versioned snapshots, 4 layout presets, 3 delivery modes (hosted/iframe/native), static previews, response lifecycle (review status ⟂ publish status), moderation. Five-layer arch: `forms-core` (contracts/compilers) + new `forms-renderer` (React) + `forms-embed` + `forms_runtime` + api_v2 + web studio. | Forms rebuild kickoff |
| 2026-06-18 | The forms rebuild swaps the schema destructively (drop + recreate); no data migration/backfill.                                                                                                                            | Pre-launch: the live `forms:backfill-v4` was never run; all forms data is local/dev. Authorized by the "delete every instance … down to the DB level … full rebuild from scratch" directive. Replaces `CollectionForm`/`CollectionFormSubmission`(+annotation/private-metadata)/`SubmissionModerationRun`/`FormImpression`/`StudioDraft`(FORM half) with `Form`/`FormVersion`/`FormResponse`(+private-metadata/annotation)/`FormModerationRun`/`FormView`. `FormResponse` becomes the canonical feedback record read by widgets/exports/analytics/billing/projects (supersedes the 2026-06-03 `CollectionFormSubmission`-is-canonical decision). | Forms rebuild kickoff |
| 2026-06-18 | A single React `forms-renderer` renders the published snapshot everywhere; the forms-v4 "no-JS, <3 KB script-free shadow embed" constraint is retired.                                                                       | One renderer powers dashboard preview, hosted SSR pages, iframe embeds, native Shadow-DOM injection, and static-preview capture (spec §27 preview-accuracy rule). Embeds ship as `embed.js` (iframe + height postMessage) and `loader.js`/`<semblia-form>` (web component mounting the renderer in a shadow root). | Forms rebuild kickoff |
| 2026-06-18 | Forms hosted/embed addressing is `forms.semblia.com/f/:slug` and `/embed/:slug`; the per-project `*.collect.semblia.com` host model is retired for forms. API routes stay under `/v2/...` with multiple forms per project (`/v2/projects/:slug/forms/:formId/...`). | Maps the spec's `/api/...` + `forms.semblia.com` to repo conventions. Shared infra preserved: `PublicSurfaceHost`, `ProjectTrustedOrigin`, `ProjectSigningSecret`, `MediaAsset`, `PublicSubmitIdempotency`, `Widget`, analytics tables, and the widget half of `StudioDraft`. | Forms rebuild kickoff |
| 2026-07-04 | Hosted walls ship as a public, indexable web_v2 route at `/wall/:wallSlug` (canonical `semblia.com/wall/:slug`), un-deferring the 2026-06-14 hosted-wall clause. | The page renders the same widgets-core SSR fragment embeds use (one renderer, every surface) inside a theme-derived shell, with per-page robots override (app stays noindex), canonical/OG metadata, and Organization+AggregateRating+Review JSON-LD. The public wall payload carries `project { name, websiteUrl }`. Wall URLs centralize in `lib/semblia-urls.ts`. | Forms/widgets/walls overhaul session |
| 2026-07-04 | Widget layout variety ships as `layout.variant` inside the definition doc — never as new `LayoutType` enum values. | Variants are CSS-only recompositions scoped by `data-sw-variant`, defaulted to `classic` (old docs parse unchanged, no migration); unknown variants normalize to classic at render. Adding a variant touches only widgets-core. | Forms/widgets/walls overhaul session |
| 2026-07-04 | Creation flows seed a "starting look/style": curated design seeds share one naming family across forms and widgets (Your brand / Clean / Editorial / Launch / Soft / Mono / Noir, + widget-only Slate / Forest) and are applied client-side as the first server draft right after create. | SUPERSEDED 2026-07-13: the "look/preset" model died with the template system; the starting choice is now the template itself. | Forms/widgets/walls overhaul session |
| 2026-07-13 | Forms + widgets are a **white-label template system**, not a builder. A template is a self-contained design project (own composition, typography, motion, iconography, loader, success moment) with a niche ritual; the roster grows over time, the knobs per template never do. | Supersedes the presentation half of forms-v4 parametric knobs (2026-06-10) and the 4-layout-preset model (2026-06-18). Contract halves: manifests in `forms-core/src/templates.ts` + `widgets-core/src/templates.ts` (identity, ≤3 accent decisions, theme recipe, pacing, asset slots); render packs beside their renderer (`forms-renderer/src/templates/<id>/`, widgets-core render). Decision doc: `docs/ui-rework/2026-07-13-forms-widgets-template-rebuild/decision.md`; principles P1–P10 in the same directory. | Template system rebuild |
| 2026-07-13 | Owners contribute **brand facts + words only**: logo, one brand color (AA-clamped derivation, now including accent-vs-background 1.4.11 clamping in brand-theme), business name, appearance (clamped to what the template supports), template-declared imagery slots, and all copy. Layout, pacing, fonts, radii, density, surfaces, and consent placement are template-owned; no raw appearance knob may return to an owner surface. | Doc contracts: `FormDefinitionDoc` v6 (`schemaVersion 6.0.0`) and `WidgetDefinitionDoc` v2. Older docs project forward lossily (brand facts + content/fields/rules survive; knob picks drop). Launch roster: forms Meridian/Aperture/Ledger/Parcel/Terminal (intent-defaulted), widgets Marquee/Gallery/Mosaic/Column/Editorial. | Template system rebuild |
| 2026-07-13 | Media is optimized server-side: an api_v2 `media-optimize` BullMQ worker derives width-tiered WebP variants (320/640/1280/2560, never upscaled) when an asset becomes ACTIVE, recorded on `MediaAsset.derivatives`/`optimizedAt`. Video/audio pass through the same queue seam; transcode/poster extraction is the named follow-up there. | Enqueues are best-effort (never fail an upload), job ids colon-free, processing idempotent. Derivative keys extend the original storage key so they inherit its public/private root. Serving-side selection uses `bestDerivativeKey`; wiring fragments/DTOs onto derivatives is a follow-up pass. | Template system rebuild |
| 2026-07-14 | A template owns its skeleton and item markup, and designs every delivery surface separately: forms render `surface: hosted\|embed` (`data-tf-surface`), widgets render `surface: embed\|wall` (`data-sw-surface`); the wall masthead (h1 + proof stats) is template-owned and `/wall/:slug` hands its hero to the fragment. Two templates sharing a page structure means one of them is wrong. | Supersedes PR #44's presentation layer (rejected for card-sameness + identical surfaces). Packs size hosted pages with `--tf-viewport` (default `100svh`) so studio/preview frames can bind it. Stored widget docs/snapshots always migrate/fall forward — consumers must never hard-parse stored blobs against the current contract (`composePublishedWidgetDoc`, api `snapshotFromWidget`, `syncStudioConfig`, studio persist v4). | Template system v2 (`docs/ui-rework/2026-07-14-template-system-v2/`) |
| 2026-07-17 | **Embedded forms are a different product, not a cropped hosted form.** `FormDefinitionDoc.delivery: hosted\|embed` (additive, default hosted, mutable in Setup): embed forms have a constrained palette (no upload/capture types) and an `EMBED_MAX_FIELDS` ask cap (consent/hidden don't count), enforced at publish (422) and seeded fit at create. `/f/:slug` serves hosted-delivery only, `/embed/:slug` embed-delivery only (pre-split snapshots read as hosted). The embed ships as a hydrated iframe document (`/embed.js` → `<semblia-form project form>` loader, height postMessage, transparent body, first-party submit); shadow-DOM `loader.js` remains the Phase-8 follow-up. No DB column: the doc + snapshot carry delivery. | User directive 2026-07-17 ("fields are limited for an embedded form … that should be reflective"). Plan `docs/plans/2026-07-17-template-system-v2-product-polish.md`. | Product polish pass |
| 2026-07-17 | **Form intent is mutable.** The doc is the source of truth; `saveDraft` mirrors `draft.intent` onto the queryable `Form.intent` column (fixing the known drift). The studio Setup exposes a Form type select; changing type never reseeds fields. | User directive ("they might make a mistake, or change their minds later"). | Product polish pass |
| 2026-07-17 | **Protection, consent, and anonymity are platform-owned** — no studio controls. Captcha/min-time/honeypot/blocked-words UI and the consent/anonymous toggles are gone; platform defaults lean protective (`captchaMode` default `suspicious`). The `consent` field type left the palette and the outline (still seeded per intent, rendered, and validated — respondents keep the consent moment; owners don't manage it as a field). Contract shapes unchanged (old docs parse). | User directive ("anonymity, consent, captcha … that's our side of things"). | Product polish pass |
| 2026-07-17 | **Owners get non-structural token control ("Finish"): corner radius, density, surface style — over the template recipe, never the structure.** `tuning` on both docs (forms + widgets; null = template default), merged by `applyThemeTuning` (@workspace/brand-theme) at every derivation chokepoint, still AA-clamped. Shared `FinishSection` panel in the forms Brand tab and widgets Style tab. Raw 9-knob theming stays dead — this is a 3-knob finish layer, not a return of presets. | User directive 2026-07-17 ("give token control on things like corner radius, etc. Those are not destructive on the structure"). | Template refinement pass |
| 2026-07-17 | **The refinement canon governs pack styling.** `docs/ui-rework/2026-07-17-template-refinement/research.md` (measured from Senja/Testimonial.to/Tally/Trustmary/Trustpilot live surfaces) is the authority for card anatomy, star treatment (one amber slot, omitted when unrated), quote clamping (no glyphs outside editorial devices), avatar fallbacks (designed per-name tint), elevation (hairline or whisper shadow), masonry rhythm, and collection-form psychology (huge rating hit areas, guidance prompts, recorder reassurance). New/edited packs must comply; the specs live beside it in `specs.md`. | Template refinement pass (user: "templates need to be more refined, and better represent a feedback system"). | Template refinement pass |
| 2026-07-17 | **Fixed-height studio frames are real viewports** — pages taller than the device scroll inside the frame (never a silent mid-element clip). Height-fluid (embed) frames keep hug-content behavior. | Template refinement pass (the "overflows still exist" audit). | Template refinement pass |
| 2026-07-17 | **Studios: content lives LEFT, design lives RIGHT.** Selecting a field/section swaps the LEFT rail to its editor (`StudioFrame.outlineOverride`); the right inspector is never hijacked. Forms right tabs = Template · Brand · Setup; conditional rules are per-field Logic sections (content). Widgets gain a left content rail; right = Layout · Style. Previews are showcases (renderer `mode: "showcase"` — inert fields, free step navigation, success moment on demand); embed-delivery previews always sit inside `HostPageChrome`; embed canvas frames are fit-height (an embed is an in-flow element, not a page). Widgets never render headings on the embed surface (masthead is wall-only) and never paint a scope background on embeds. | User review of PR #45 (2026-07-17). Textareas are never resizable app-wide (web_v2 primitive + `.tf-textarea`). | Product polish pass |

## Retired Decisions

Resolved or superseded decisions are intentionally pruned from the active table. Older plans and handoff docs remain historical evidence only; when they conflict with this file, `progress.md`, or `open-questions.md`, the continuity docs win.
