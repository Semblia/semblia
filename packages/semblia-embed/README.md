# @semblia/embed

Internal loader that injects and dedupes the Semblia CDN embed scripts
(`widgets.semblia.com/embed.js`, `forms.semblia.com/embed.js`). Bundled into
`@semblia/react`; published standalone once a non-React npm audience exists —
extraction is publish-config work, not a refactor, by design.
